{% for flashMessage in app.session.flashbag.get('notice') %}
    <div class="flash-notice" style="background-color: indianred; padding: 10px; margin-bottom: 5px">
        {{ flashMessage }}
    </div>
{% endfor %}

{# Optionally, turn off validation by setting the "novalidate" attribute #}
{# Note: Clear your cache for effects to take place! (app/console ca:cl) #}
{# {{ form(form, {'attr': {'novalidate': 'novalidate'}}) }} #}
{{ form_start(form) }}
{{ form_widget(form) }}
{{ form_end(form) }}